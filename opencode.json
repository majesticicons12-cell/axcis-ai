{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "react-grab": {
      "type": "local",
      "command": ["npx", "-y", "@react-grab/mcp", "--stdio"],
      "enabled": true
    }
  }
}
